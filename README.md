# RMMV Formula Visualizer

A standalone damage formula debugging and visualization tool for numerical designers, featuring the classic RPG Maker MV window and dialog style.

## Key Features
* Supports RMMV formula syntax (such as a, b, item variables).
* Automatically generates control sliders and checkboxes based on parsed variables and states in the formula.
